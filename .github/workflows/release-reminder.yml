name: Release Reminder

on:
  schedule:
    - cron: '0 12 * * *'
  workflow_dispatch:

permissions: {}

jobs:
  reminder:
    permissions:
      contents: read
      issues: write
      pull-requests: write
    uses: projectbluefin/actions/.github/workflows/reusable-release-reminder.yml@4d46a63f49ed0159e2f5958a2109cc936a50417e # v1
    with:
      days_warn: 7
      days_escalate: 14
    secrets: inherit

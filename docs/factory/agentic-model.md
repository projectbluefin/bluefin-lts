---
name: agentic-model
description: Canonical location has moved to projectbluefin/common.
---

# Agentic Operating Model

> **This file is a stub.** The canonical agentic-model lives in
> [`projectbluefin/common`](https://github.com/projectbluefin/common/blob/main/docs/factory/agentic-model.md).
>
> Load that file. Do not edit this one — it will be deleted once all agents
> reference the canonical source.

The local copy diverged from common and caused agents to use stale branch targets
(e.g. dakota targeting `main` instead of `testing`). Single source of truth is common.
